pipeline {
  agent {
    docker {
      image 'node:16-alpine'
      args '-v /var/jenkins_home:/app -w /app' // Setting working directory and mounting volume
    }
  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
  }
}
