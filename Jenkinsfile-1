pipeline {
  agent {
    docker { 
      image 'node:16-alpine'
      args '-v /var/jenkins_home:/app' // Mounting Jenkins workspace to /app inside container
    }
  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
  }
}
